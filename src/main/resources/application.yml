spring:
  jpa:
    datasource:
      url: jdbc:h2:mem:testdb
    sql:
      init:
        mode: never
    h2:
      console:
        enable: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true